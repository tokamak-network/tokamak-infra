apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

commonLabels:
  app: graph-node

configMapGenerator:
  - name: graph-node
    envs:
      - graph-node.env

secretGenerator:
  - name: secret
    envs:
      - secret.env
