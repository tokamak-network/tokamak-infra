# Secret-scanning gate. Install: `pip install pre-commit && pre-commit install`
# Run on the whole tree once: `pre-commit run gitleaks --all-files`
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
